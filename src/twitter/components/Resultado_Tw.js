import React from "react";

const Resultado_Tw = (props) => {

    return (
        <>
            <h1>En esta página se mostrarán las gráficas y el archivo 
                de Excel que se descargará (El codigo aun está en Twitter.js).
            </h1>
        </> 
    );
}

export default Resultado_Tw;